import { LayoutProps } from "antd";
import Layout from "antd/lib/layout/layout";
import styled from "styled-components";

export const StyledLayout: React.FunctionComponent<LayoutProps> = styled(
  Layout
)`
  background: unset !important;
`;
